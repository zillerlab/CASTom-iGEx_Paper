library(clusterProfiler)
require(ReactomePA)

fold <- "/psycl/g/mpsziller/lucia/CAD_UKBB/eQTL_PROJECT/OUTPUT_GTEx/predict_CAD/AllTissues/200kb/CAD_GWAS_bin5e-2/UKBB/"
pheno_name <- "CAD_HARD"
pathR_assoc <- read.delim(sprintf("%s/path_Reactome_pval_%s_covCorr_filt.txt", fold, pheno_name), 
                          sep = "\t", h=T, stringsAsFactors = F)

#### TO DO #####
# - use genes relibale in a tissue as background
# - use enrichPathway and enrichGO but genes id need to be converted, can we trust the set of genes is the same?
# - otherwise, set-up a fisher.test (alternative = greater) given the set of pathways in (filt) and all the available results

x <- enrichPathway(gene=de, pvalueCutoff = 0.05, readable=TRUE)
head(x)
